(100%, 300px);
      aspect-ratio: 1;
      display: block;
      border-radius: 8px;
      background: var(--cream);
      box-shadow: 0 0 0 3px var(--cream), 0 8px 28px rgba(0,0,0,0.35);
    }

    .tour-caption {
      width: 100%;
      text-align: center;
      font-size: clamp(0.72rem, 1vw, 0.92rem);
      line-height: 1.35;
      color: var(--cream);
      letter-spacing: 0.04em;
      font-weight: 560;
    }

    .tour-caption .addr {
      display: block;
      margin-top: 4px;
      font-size: clamp(0.62rem, 0.85vw, 0.78rem);
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--terracotta);
      font-weight: 500;
    }

    .cal-col {
      flex: 1;
      min-width: 0;
      min-height: 0;
      display: flex;
      flex-direction: column;
      gap: clamp(8px, 1vh, 14px);
    }

    /*: Header: */
    header.top {
      display: grid;
      grid-template-columns: 1fr auto 1fr;
      align-items: end;
      gap: 16px;
      flex-shrink: 0;
      border-bottom: 1px solid var(--border-strong);
      padding-bottom: clamp(10px, 1.2vh, 16px);
    }

    .brand {
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .brand .name {
      font-family: var(--font);
      font-size: clamp(1.15rem, 2.1vw, 1.85rem);
      font-weight: 600;
      letter-spacing: 0.02em;
      color: var(--cream);
      line-height: 1.1;
    }

    .brand .loc {
      font-size: clamp(0.7rem, 1vw, 0.9rem);
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--terracotta);
      font-weight: 500;
    }

    .month-title {
      text-align: center;
      font-family: var(--font);
      font-size: clamp(1.4rem, 2.8vw, 2.4rem);
      font-weight: 600;
      color: var(--text);
      letter-spacing: 0.01em;
      line-height: 1.15;
    }

    .month-title .sub {
      display: block;
      font-family: var(--sans);
      font-size: clamp(0.65rem, 0.95vw, 0.8rem);
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--muted);
      font-weight: 500;
      margin-top: 4px;
    }

    .clock-wrap {
      justify-self: end;
      text-align: right;
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 2px;
    }

    #clock {
      font-family: var(--font);
      font-size: clamp(1.35rem, 2.4vw, 2.1rem);
      font-variant-numeric: tabular-nums;
      color: var(--cream);
      letter-spacing: 0.04em;
      line-height: 1;
    }

    #clock-date {
      font-size: clamp(0.65rem, 0.9vw, 0.78rem);
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--cream-dim);
    }

    .weather-strip {
      display: flex;
      align-items: center;
      justify-content: flex-end;
      gap: 7px;
      max-width: min(30vw, 250px);
      margin-top: 3px;
      font-size: clamp(0.62rem, 0.82vw, 0.74rem);
      line-height: 1.15;
      letter-spacing: 0.03em;
      white-space: nowrap;
      color: var(--cream-dim);
    }

    .weather-strip[hidden] {
      display: none;
    }

    .weather-main {
      overflow: hidden;
      text-overflow: ellipsis;
      color: var(--cream);
      font-weight: 560;
    }

    .weather-range {
      color: var(--cream-dim);
    }

    /*: Weekday labels: */
    .weekdays {
      display: grid;
      grid-template-columns: repeat(7, 1fr);
      gap: clamp(4px, 0.5vw, 8px);
      flex-shrink: 0;
      padding: 0 2px;
    }

    .weekday {
      text-align: center;
      font-size: clamp(0.62rem, 0.85vw, 0.78rem);
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--cream-faint);
      font-weight: 600;
      padding: 2px 0;
    }

    /*: Month / week grid: */
    .grid {
      flex: 1;
      min-height: 0;
      display: grid;
      grid-template-columns: repeat(7, 1fr);
      gap: clamp(4px, 0.55vw, 8px);
    }

    .grid.month {
      grid-template-rows: repeat(6, 1fr);
    }

    .grid.week {
      grid-template-rows: 1fr;
    }

    .day {
      background: var(--bg-cell);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: clamp(6px, 0.7vw, 10px) clamp(6px, 0.65vw, 10px) clamp(4px, 0.5vw, 8px);
      display: flex;
      flex-direction: column;
      min-height: 0;
      overflow: hidden;
      position: relative;
      transition: border-color 0.2s ease;
    }

    .day.outside {
      background: var(--bg-cell-mute);
      opacity: 0.42;
    }

    .day.today {
      background: var(--today-bg);
      border-color: var(--terracotta);
      box-shadow:
        0 0 0 1px var(--terracotta-glow),
        inset 0 0 40px rgba(196, 120, 90, 0.06);
    }

    .day.has-events:not(.outside) {
      border-color: rgba(232, 220, 200, 0.12);
    }

    .day-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      margin-bottom: clamp(4px, 0.5vh, 8px);
      flex-shrink: 0;
    }

    .day-num {
      font-family: var(--font);
      font-size: clamp(0.95rem, 1.5vw, 1.35rem);
      font-weight: 600;
      color: var